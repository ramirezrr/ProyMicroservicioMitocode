spring:
  application:
    name: authentication-servet-jwt

  cloud:
    config:
      uri: http://localhost:9000
      profile: dev
      enabled: true

# Eureka Client Configuration
eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka